
for each (var r in results) {
    for each (var o in r.obx) {
    	logger.info(o.segmento);
        /*finalJson.procedimientos.push({
            Prueba: o.codigo,
            IDProcedimiento: o.codigo,
            Procedimiento: r.perfil, // nombre de la prueba del OBR
            Estatus: "Validado"
        });*/
        
    }
}
