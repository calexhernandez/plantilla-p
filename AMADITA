try {
    var clientClass = Packages.org.example.SocketClient;
    logger.info("✅ Clase cargada correctamente: " + clientClass);
} catch (e) {
    logger.error("❌ Error cargando clase SocketClient: " + e);
}
